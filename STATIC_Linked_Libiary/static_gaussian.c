#include "static_gaussian.h"

void gaussian_calculation(FILE * instream) {
	//declarations
	float expected_value = 0.0;
	float std_deviation = 0.0;

	//print output
	printf("\nexpected value: %.2f, standart deviation: %.2f", expected_value, std_deviation);
}
